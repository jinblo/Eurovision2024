import { useState } from "react";
import { StyleSheet, Text, View } from 'react-native';

export default function Scoreboard({ route, navigation }) {
  const [score, setScore] = useState();

  return (<Text>Scoreboard</Text>)
}
